Preffer that the categories tab in the headerbar sit in a second row. On the first row should stay the logo, and the single use pages, like despre, contact annd doneaza. The first row can not overflow on new rows, but the second can.

I also want that the Despre, Contact and Doneaza pages be treated differently, to not be shown in the list of articles (and also not be shown in any archive, or list of a category). I want them to have a custom layout, similar to a simple one page, without date, author etc.

Also I want a simple variable where I can modify the width, in order to utilise more web space on PC (not full width, but something bigger then current, and always some space compared to the max width of the page). This also fixes a bug where on mobile the width is from one end to the other of available space.

Small bug, the footer has pure white background and it doesn't use the canonical off-white background color.

Also is there a way to in the future reddirect the Doneaza button to an expernal page instead to a page on the site?
